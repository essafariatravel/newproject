PRODUCTION RELEASE AUTHORIZATION — visa_os

Authorized by: the repository owner, on 2026-09-24, in response to the
"PRODUCTION PRE-FLIGHT REPORT — release 0013 → 0017" (docs/PRODUCTION-PREFLIGHT-REPORT.md).

Authorized scope (nothing else):
  - schema:            visa_os (Production)
  - baseline/app code: 8f04bb0 (hosted-verified)
  - release tooling:   5d5418e (+ the owner-approved transaction-local probe fix)
  - migrations:        0013_embassy_applicability.sql
                       0014_wallet_topup_requests.sql
                       0015_schema_safe_references.sql
                       0016_document_type_audience.sql
                       0017_decision_types_audience.sql
  - then:              deploy the verified application baseline to Production.

Preconditions verified before this file was added:
  - pre-write read-only audit: PREFLIGHT VERDICT READY, ledger 0001-0012,
    protected counts / wallet checksum / agency balances identical to the
    approved baseline (run 35996483659, commit 2cd9f2f).

This file is the one-time arming sentinel for the guarded Production release
workflow (.github/workflows/prod-release.yml). It is deliberately removed again
immediately after a successful apply, so it can never authorize a later release.
Removing it is part of the release procedure, not an error.
